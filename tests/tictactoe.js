'use strict';

describe('tictactoe', function() {
  beforeEach(module('TicTacToe'));

  describe('what is happening', function() {
    console.log('did it work?');
    it('should match', function() {
      expect(1).toBe(2);
    });
  });
});
